#include<iostream>
#include<vector>
#include<maths>
using namespace std;
int minimunDay(vector<int> arr,int n,int m , int k){
    int mini = min(arr.begin(),arr.end());
    int maxi = max(arr.begin(),arr.end());
    
}
int main(){
    vector<int> arr{1};
    int n = arr.size();
    int m = 2;
    int k = 3;

     minimunDay(arr,n , m,k);
    // cout<<"Minimun Element = "<<ans<<endl;
    return 0; 
}